fs() {
    #echo $1;
    #echo $2;

	sed 's/'$2'//g' $1 > ${3:-$1_filtered};
}

# this works!
# echo 'example bla bla example' | sed 's/bla //g'

# filename > contents
readfile() {
	cat $1;
}

# filename, string > void
writefile() {
	echo $2 > $1;
}

